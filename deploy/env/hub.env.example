# Sentinel hub env — copy to /etc/sentinel/hub.env, chown
# sentinel:sentinel, chmod 0600 (it contains a DB password).
# Loaded by sentinel-hub.service and sentinel-daemon.service.
# The agent needs no DATABASE_URL at all.

DATABASE_URL=postgresql://sentinel:CHANGE-ME@127.0.0.1/sentinel
